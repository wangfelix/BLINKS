# Expo / React Native (Continuous Native Generation — native dirs are generated)
node_modules/
.expo/
dist/
web-build/
npm-debug.*
*.orig.*

# Native projects: generated by `expo prebuild` / `expo run` — do NOT commit,
# or config plugins (kotlinVersion pin, BLE background mode) stop auto-applying.
/android
/ios

# Misc
.DS_Store
*.log
.env*.local
